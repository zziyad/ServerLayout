// =============================================================================
// USER REPOSITORY - FindRole
// =============================================================================

async (roleId) => db.pg.row('Role', ['id', 'name', 'is_system', 'is_active', 'is_deleted'], {
  id: roleId,
});
